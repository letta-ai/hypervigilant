# Required only when [connection].backend is "cloud"
LETTA_API_KEY=sk-let-xxxxxxxxxxxxxxxxxxxxxxxx

# Remote App Server bearer token (required for non-loopback servers)
# LETTA_APP_SERVER_TOKEN=replace-me

# Generic HTTP event receiver bearer token (use the name configured in destinations.http)
# HYPERVIGILANT_STREAM_TOKEN=replace-me
